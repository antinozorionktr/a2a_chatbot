CMD bash -c "\
    set -a && source .env && set +a && \
    python -u multi_agent.py both > backend.log 2>&1 & \
    echo '🕒 Waiting for backend...' && \
    sleep 15 && \
    echo '🚀 Starting frontend...' && \
    streamlit run frontend.py --server.port=8501 --server.address=0.0.0.0"
